//! Periwinkle / slate colour constants shared with review-dialog and settings.

use eframe::egui::Color32;

/// Periwinkle accent (primary action buttons, active state indicators).
pub(crate) const PERIWINKLE: Color32 = Color32::from_rgb(0x7c, 0x83, 0xfd);
/// Slate (secondary / muted text, disabled items).
pub(crate) const SLATE: Color32 = Color32::from_rgb(0x94, 0xa3, 0xb8);
/// Text primary.
pub(crate) const TEXT: Color32 = Color32::from_rgb(0xcd, 0xd6, 0xf4);
